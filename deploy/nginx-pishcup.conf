# Place at /etc/nginx/sites-available/IranOpen-InSitu.yourdomain.com
# then: ln -s ../sites-available/IranOpen-InSitu.yourdomain.com /etc/nginx/sites-enabled/
# then: nginx -t && systemctl reload nginx
# then: certbot --nginx -d IranOpen-InSitu.yourdomain.com   (adds TLS, doesn't touch other sites)

server {
    listen 80;
    server_name IranOpen-InSitu.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
